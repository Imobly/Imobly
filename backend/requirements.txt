# ============================================================
# Dependências de PRODUÇÃO.
# Ferramentas de teste e lint ficam em requirements-dev.txt: instaladas aqui,
# inflavam a imagem de produção e ampliavam a superfície de ataque sem uso.
# ============================================================

# Framework web
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Banco de dados
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
alembic==1.13.1

# Supabase (Auth + Storage) - Versões compatíveis
supabase==2.4.4
gotrue==2.8.0
httpx==0.24.1

# Validação de JWT. `python-jose` foi removido: estava declarado mas nunca
# importado — só o PyJWT é usado (src/security.py).
#
# O extra `[crypto]` é OBRIGATÓRIO, não opcional: sem ele o PyJWT nem expõe
# ES256/RS256, e projetos novos do Supabase assinam os tokens com ES256. Sem
# o extra, o login funciona e TODA requisição autenticada depois dele
# responde 401 — porque a chave pública do JWKS não pode ser carregada.
PyJWT[crypto]>=2.8.0

# Validação de dados e settings
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0

# Upload de arquivos e utilitários
python-multipart==0.0.6
python-dotenv==1.0.0
pillow==10.1.0
python-dateutil==2.8.2

# Agendamento de tarefas
apscheduler==3.10.4

# Rate limiting das rotas de autenticação
slowapi==0.1.9

# Cache com TTL e teto de tamanho (resolução de identidade)
cachetools==5.3.2
